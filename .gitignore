node_modules
.next
out
build
.DS_Store
*.pem

# Environment — never commit your API key
.env
.env.local
.env*.local
# Backups and copies of env files — these hold live keys
.env*.bak
*.env.bak
.env.local.*

.vercel
next-env.d.ts
*.tsbuildinfo

# Per-machine editor/tool settings — contains local filesystem paths
.claude/settings.local.json

# Copies of the OCR runtime, written by scripts/copy-ocr-assets.mjs from
# node_modules. public/ocr/lang/ is committed — see that script for why.
/public/ocr/*
!/public/ocr/lang/
